{
  "name": "webrtc-large-file-drop",
  "version": "1.0.0",
  "private": true,
  "description": "Manual-signaling WebRTC large file transfer with chunk hashing and resumable disk writes.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 scripts/serve-https.py"
  }
}
